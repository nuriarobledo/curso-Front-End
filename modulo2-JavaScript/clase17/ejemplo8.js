const nombre = 'Nuria';
const edad= 20;

console.log(`hola! ${nombre}, asi que tienes ${edad} años.`);

const edad1= 18;
const edad2 = 31;

const mensaje1= `la cuota para personad de ${edad1} es de ${edad >=21 ? 500 : 300}` // ? camino del verdadero, : camino falso
const mensaje2= `la cuota para personas de ${edad} es de ${edad >=21 ? 500 : 300}`;
// variable condicion 
//? true
// : false 
console.log(mensaje1);
console.log(mensaje2);